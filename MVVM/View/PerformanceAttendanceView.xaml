<UserControl x:Class="SchoolManager.MVVM.View.PerformanceAttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolManager.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <local:AbsenceConverter x:Key="AbsenceConverter"/>
            <local:RespectfulAbsenceConverter x:Key="RespectfulAbsenceConverter"/>
            <local:AttendanceConverter x:Key="AttendanceConverter"/>
            <local:AbsencesCountConverter x:Key="AbsencesCountConverter"/>
            <local:RespectfulAbsencesCountConverter x:Key="RespectfulAbsencesCountConverter"/>
            <local:TotalAbsencesCountConverter x:Key="TotalAbsencesCountConverter"/>
            <local:AverageGradeConverter x:Key="AverageGradeConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Theme/ScrollBarTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Успеваемость и посещаемость" 
                   FontFamily="/Fonts/#Stolzl Book"
                   Foreground="White"
                   FontSize="26"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   Margin="0,20,0,10"/>
        <!-- Фильтры -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,5,10,5">
            <TextBlock Text="Уч. год:" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding AcademicYears}" 
                      SelectedItem="{Binding SelectedAcademicYear, Mode=TwoWay}" 
                      Width="140" Margin="5"
                      Style="{StaticResource ComboBoxTheme}"/>
            <TextBlock Text="Четверть:" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Quarters}" 
                      SelectedItem="{Binding SelectedQuarter, Mode=TwoWay}" 
                      Width="55" Margin="5"
                      Style="{StaticResource ComboBoxTheme}"/>
            <TextBlock Text="Класс:" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Classes}" 
                      SelectedItem="{Binding SelectedClass, Mode=TwoWay}" 
                      DisplayMemberPath="Name" 
                      Width="65" Margin="5"
                      Style="{StaticResource ComboBoxTheme}"/>
            <TextBlock Text="Предмет:" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Subjects}" 
                      SelectedItem="{Binding SelectedSubject, Mode=TwoWay}" 
                      DisplayMemberPath="Name" 
                      Width="140" Margin="5"
                      Style="{StaticResource ComboBoxTheme}"/>
        </StackPanel>
        <!-- Таблица -->
        <DataGrid x:Name="PerformanceAttendanceDataGrid" 
                  Grid.Row="2"
                  Margin="10"
                  AutoGenerateColumns="False" 
                  Background="Transparent"
                  RowHeight="60"
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  ColumnHeaderHeight="40"
                  BorderBrush="Transparent"
                  HorizontalGridLinesBrush="#808080"
                  VerticalGridLinesBrush="#808080"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  SelectionUnit="Cell"
                  FrozenColumnCount="1"
                  Visibility="{Binding IsDataLoaded, Converter={StaticResource BooleanToVisibilityConverter}}"
                  PreviewMouseWheel="DataGrid_PreviewMouseWheel">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Column.DisplayIndex}" Value="0">
                            <Setter Property="Background" Value="#3B4A80"/>
                            <Setter Property="BorderBrush" Value="#808080"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="#5065A6"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#3B4A80"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=., Converter={StaticResource AbsenceConverter}}" Value="True">
                            <Setter Property="Background" Value="Red"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=., Converter={StaticResource RespectfulAbsenceConverter}}" Value="True">
                            <Setter Property="Background" Value="Yellow"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Column.DisplayIndex}" Value="0">
                            <Setter Property="Background" Value="#3B4A80"/>
                            <Setter Property="BorderBrush" Value="#808080"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                    <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
        </DataGrid>
        <!-- Итоги -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="10"
                    Visibility="{Binding IsDataLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Все пропуски: " Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalAbsencesCount}" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Неуважительных: " Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalUnexcusedAbsencesCount}" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Уважительных: " Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalExcusedAbsencesCount}" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <TextBlock Text="Средний балл: " Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalAverageGrade, StringFormat={}{0:F2}}" Style="{StaticResource TextBlockTheme}" VerticalAlignment="Center" Margin="0,0,10,0"/>
        </StackPanel>
        <!-- Всплывающее окно -->
        <Popup x:Name="EditPopup"
       IsOpen="{Binding IsEditPopupOpen, Mode=TwoWay}"
       Placement="Center"
       AllowsTransparency="True">
            <Border Background="#2F2F4F"
            BorderBrush="#50508A"
            BorderThickness="1"
            CornerRadius="10"
            Padding="20"
            Width="400">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Заголовок -->
                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="Редактирование посещаемости" 
                       FontSize="18" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"
                       Style="{StaticResource TextBlockTheme}" />
                    <!-- Посещаемость -->
                    <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="Посещаемость:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,5"
                       Style="{StaticResource TextBlockTheme}"/>
                    <ComboBox x:Name="PresenceComboBox"
                      Style="{StaticResource ComboBoxTheme}"
                      Grid.Row="1" Grid.Column="1"
                      Margin="0,0,0,5"
                      SelectionChanged="PresenceComboBox_SelectionChanged">
                        <ComboBoxItem Content="Присутствовал"/>
                        <ComboBoxItem Content="Отсутствовал"/>
                    </ComboBox>
                    <!-- Оценка -->
                    <TextBlock Grid.Row="2" Grid.Column="0"
                       Text="Оценка:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,5"
                       Style="{StaticResource TextBlockTheme}"/>
                    <ComboBox x:Name="GradeComboBox"
                      Style="{StaticResource ComboBoxTheme}"
                      Grid.Row="2" Grid.Column="1"
                      Margin="0,0,0,5">
                        <ComboBoxItem Content="5"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="1"/>
                    </ComboBox>
                    <!-- Причина -->
                    <TextBlock Grid.Row="3" Grid.Column="0"
                       Text="Причина:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,5"
                       Style="{StaticResource TextBlockTheme}"/>
                    <ComboBox x:Name="ReasonComboBox"
                      Style="{StaticResource ComboBoxTheme}"
                      Grid.Row="3" Grid.Column="1"
                      Margin="0,0,0,15">
                        <ComboBoxItem Content="Уважительная"/>
                        <ComboBoxItem Content="Неуважительная"/>
                    </ComboBox>
                    <!-- Кнопки -->
                    <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0">
                        <Button Content="Сохранить"
                        Width="100"
                        Height="40"
                        Margin="0,0,10,0"
                        Click="SaveButton_Click"
                        Style="{StaticResource LoginButtonTheme}"/>
                        <Button Content="Отменить"
                        Width="100"
                        Height="40"
                        Click="CancelButton_Click"
                        Style="{StaticResource LoginButtonTheme}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>